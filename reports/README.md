# Reports

`benchmark_results.csv` and `benchmark_f1_comparison.png` are a **captured GPU run** from the notebook (the heavy models need a GPU). Regenerate the CPU-only subset with `python -m src.benchmark --skip-heavy`, or the full table with `python -m src.benchmark` on a GPU machine.
