__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Data & artifacts (regenerate via the pipeline; keep the source zip if small)
data/extracted/
artifacts/*.joblib
*.ipynb_checkpoints

# Keep these tracked:
!artifacts/.gitkeep
!data/raw/.gitkeep
.DS_Store

# Large dataset — download from Kaggle, never commit
data/raw/*.zip
*.egg-info/
